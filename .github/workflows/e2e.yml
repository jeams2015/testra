name: E2E Tests

# Corre la suite de tests E2E — on-demand o al abrir un PR hacia main.
on:
  workflow_dispatch:
    inputs:
      spec:
        description: "Spec específico a correr (vacío = toda la suite)"
        required: false
        default: ""
  pull_request:
    branches: [main]
    paths:
      - "tests/**"
      - "adapters/**"
      - "core/**"

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Typecheck
        run: npm run typecheck

      - name: Run tests
        env:
          APP_BASE_URL: ${{ vars.APP_BASE_URL }}
          APP_ENV: ${{ vars.APP_ENV }}
        run: |
          if [ -n "${{ github.event.inputs.spec }}" ]; then
            npx playwright test "${{ github.event.inputs.spec }}"
          else
            npx playwright test tests/features/
          fi

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
